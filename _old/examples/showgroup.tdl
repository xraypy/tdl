
def showgroup(group): 
  """
  show a groups variable:
  example:
      tdl> showgroup('_main')

  """
  print " === Group %s === " % group	
  for i in dictitems(group2dict(group)): 
      print "  %-10s = " % i[0], i[1]
  endfor  
enddef
